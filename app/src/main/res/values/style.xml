<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="SplashTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item> <!-- Latar belakang putih -->
        <item name="android:statusBarColor">@android:color/white</item> <!-- Status bar putih -->
        <item name="android:navigationBarColor">@android:color/white</item> <!-- Navigasi bawah putih -->
        <item name="android:windowLightStatusBar">true</item> <!-- Ikon status bar jadi hitam -->
        <item name="android:windowDisablePreview">true</item>
    </style>

</resources>